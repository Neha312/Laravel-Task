{{-- hasOne relation --}}
{{-- @foreach ($data as $d)
    {{ $d }}
@endforeach --}}

{{-- belongTo relationship --}}
{{-- @foreach ($ans as $a)
    {{ $a }}
@endforeach --}}

{{-- @foreach ($result as $r)
    {{ $r }}
@endforeach --}}
{{ $result }}
